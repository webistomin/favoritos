import * as pkg from '../../package.json';

export const BANNER_TEXT = `/**
* ${pkg.name}
*
* @version ${pkg.version}
* @author ${pkg.author}
* @email: ${pkg.email}
* @license: ${pkg.license}
*
**/\n\n`;
